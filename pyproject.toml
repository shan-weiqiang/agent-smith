[project]
name = "agent-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "anthropic>=0.89.0",
    "click>=8.1.8",
    "dotenv>=0.9.9",
]

[dependency-groups]
dev = []
